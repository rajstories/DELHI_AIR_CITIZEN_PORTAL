{
  "$schema": "https://render.com/schema.json",
  "service": {
    "name": "pollution-verification-engine",
    "runtime": "node",
    "buildCommand": "npm install && npm run build",
    "startCommand": "npm start",
    "envVars": []
  }
}
